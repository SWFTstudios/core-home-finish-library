# GitHub Actions / wrangler pages deploy (do not commit real tokens)
# CLOUDFLARE_API_TOKEN=
# CLOUDFLARE_ACCOUNT_ID=

# Figma MCP (copy to .cursor/mcp.json — do not commit real keys)
FIGMA_API_KEY=

# Optional: override dev user when testing API locally
# DEV_USER_EMAIL=pd@corehome.internal
