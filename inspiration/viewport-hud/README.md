# viewport-hud

**3D workspace is the full background**; tools are minimal overlays on the edges (Blender / viewport-first).

| File | Notes |
|------|--------|
| `blender-minimal-hud.png` | Grid floor + wireframe object; left tool list + right lighting sliders |

**Mapped in code:** `public/css/configurator.css` — `.viewport-stage` (full bleed), `.viewport-hud` (fixed overlay layer), side panels `position: fixed`.
