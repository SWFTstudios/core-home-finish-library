# floating-panels

Configurator UIs where **option cards float above** the product view (not in a scrolling page column).

| File | Notes |
|------|--------|
| `porsche-configurator.png` | Bottom shelf cards (colors / wheels / lights); top-right utility cluster |
| `cap-side-curves.png` | Curved left/right rails for category + material swatches |
| `mercedes-glass.png` | Frosted glass panel over showroom background; wheels/brakes cards on the left |

**Mapped in code:** `public/css/configurator.css` — `.hud-card`, `.graphic-shelf`, `.specs-card`, `.finish-dial-card` use glass + fixed positioning.
